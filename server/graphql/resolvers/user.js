const User = require("../../models/user");

const Query = {
    getUsers: () => {},
    getUser: (id) => {}
};

const Mutation = {
    register: (registerInput) => {},
    login: (loginInput) => {}
};

module.exports = { Query, Mutation };
